---
phase: 01-3d-workspace-configurator
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/components/workspace/CatalogSidebar.tsx
  - src/components/workspace/WorkspaceConfigurator.tsx
autonomous: true
requirements:
  - REQ-3D-02
  - REQ-3D-04
user_setup: []
---

<objective>
Build the catalog sidebar with accordion categories and item thumbnails. Implement selection logic for same-category replacement and up to 3 monitors.

Purpose: Users need to browse the furniture catalog and make selections — this is the primary interaction point.
Output: Working sidebar with 5 accordion categories, each showing a 2-column grid of item thumbnails.
</objective>

<context>
@.planning/phases/01-3d-workspace-configurator/01-RESEARCH.md (Pattern 3: Zustand, Pattern 5: shadcn Dialog, Pattern 6: Catalog Data)
@.planning/phases/01-3d-workspace-configurator/01-01-PLAN.md (Wave 1 deliverables)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CatalogSidebar component structure</name>
  <files>src/components/workspace/CatalogSidebar.tsx</files>
  <action>
Create CatalogSidebar component:
- 'use client' directive
- Fixed width 280px, light zone (bg-gray-50), border-right border-gray-200
- Uses shadcn/ui Accordion for category navigation
- 5 categories: Tables, Chairs, Monitors, Decorative, Shelves
- Preload models on category expand using useGLTF.preload()

Structure per RESEARCH.md Pattern 8 (Catalog Accordion with Thumbnails).
  </action>
  <verify>
<automated>grep -q "Accordion" src/components/workspace/CatalogSidebar.tsx</automated>
  </verify>
  <done>CatalogSidebar uses shadcn Accordion with 5 categories</done>
</task>

<task type="auto">
  <name>Task 2: Create CategorySection component</name>
  <files>src/components/workspace/CatalogSidebar.tsx</files>
  <action>
Create CategorySection component inside CatalogSidebar:
- Receives category, label, items props
- Renders 2-column grid of shadcn/ui Card components
- Each Card shows: thumbnail image, item name, price/mo
- onClick triggers useConfiguratorStore.selectItem(item)
- Visual feedback: hover ring, selected state (different border)

Items displayed from catalogData.ts.
  </action>
  <verify>
<automated>grep -q "selectItem" src/components/workspace/CatalogSidebar.tsx</automated>
  </done>
  <done>CategorySection renders item cards with thumbnails and prices</done>
</task>

<task type="auto">
  <name>Task 3: Implement selection logic with Zustand</name>
  <files>src/stores/useConfiguratorStore.ts, src/components/workspace/CatalogSidebar.tsx</files>
  <action>
Verify Zustand selectItem handles all category-specific logic:
- tables: replace previous table selection
- chairs: replace previous chair selection  
- monitors: add to array (max 3), toggle on re-click
- decorative: add to array (no limit for prototype)
- shelves: replace previous shelf selection

Ensure CatalogSidebar calls selectItem on card click and displays selected state.
  </action>
  <verify>
<automated>grep -c "case" src/stores/useConfiguratorStore.ts</automated>
  </done>
  <done>Selection logic handles: single item replacement (tables/chairs/shelves), multi-item arrays (monitors/decorative)</done>
</task>

<task type="auto">
  <name>Task 4: Integrate CatalogSidebar into WorkspaceConfigurator</name>
  <files>src/components/workspace/WorkspaceConfigurator.tsx</files>
  <action>
Update WorkspaceConfigurator to include CatalogSidebar in the layout:
- Bento box layout: flex row
- Left: CatalogSidebar (280px fixed, bg-gray-50)
- Right: ViewportCanvas (flex-1)

Follow bento layout pattern from RESEARCH.md.
  </action>
  <verify>
<automated>grep -q "CatalogSidebar" src/components/workspace/WorkspaceConfigurator.tsx</automated>
  </done>
  <done>WorkspaceConfigurator has bento layout: sidebar | viewport</done>
</task>

<task type="auto">
  <name>Task 5: Create SVG thumbnail placeholders</name>
  <files>src/components/catalog/ThumbnailPlaceholder.tsx, src/components/workspace/CategorySection.tsx</files>
  <action>
Create ThumbnailPlaceholder component that renders SVG placeholders directly in the UI:
- Simple colored rectangle with category-specific colors:
  - Tables: blue (#3B82F6)
  - Chairs: green (#10B981)
  - Monitors: purple (#8B5CF6)
  - Decorative: amber (#F59E0B)
  - Shelves: rose (#F43F5E)
- Shows item name in white text centered
- No external thumbnail files needed
- Size: 80x80px with rounded corners

Update CategorySection to use ThumbnailPlaceholder instead of img tags. Each item card shows: ThumbnailPlaceholder, item name, price/mo.
  </action>
  <verify>
<automated>grep -q "ThumbnailPlaceholder" src/components/workspace/CategorySection.tsx</automated>
  </verify>
  <done>SVG thumbnail placeholders render in catalog cards with category colors</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI→Zustand | Catalog selection updates store — no external data flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Catalog selection | accept | Static catalog data, no user-supplied values |
</threat_model>

<verification>
- Browser shows sidebar with 5 accordion categories
- Clicking category expands to show item grid
- Clicking item updates Zustand store
- Same-category selection replaces previous item
- Monitor selection allows up to 3 items
</verification>

<success_criteria>
- [x] CatalogSidebar renders with 5 accordion categories
- [x] Each category shows 2-column thumbnail grid
- [x] Clicking item triggers selectItem in Zustand
- [x] Same-category replacement works (tables/chairs/shelves)
- [x] Monitor selection supports up to 3 items
- [x] Visual selected state shown on active items
</success_criteria>

<must_haves>
  truths:
    - "Users can browse all 5 categories"
    - "Clicking an item adds it to selection"
    - "Same-category items replace previous selection"
    - "Monitors allow up to 3 selections"
  artifacts:
    - path: "src/components/workspace/CatalogSidebar.tsx"
      provides: "Accordion categories, thumbnail grids, selection interaction"
    - path: "src/components/workspace/WorkspaceConfigurator.tsx"
      provides: "Bento layout with sidebar + viewport"
  key_links:
    - from: "CatalogSidebar"
      to: "useConfiguratorStore"
      via: "selectItem action"
      pattern: "useConfiguratorStore.*selectItem"
    - from: "CatalogSidebar"
      to: "catalogData"
      via: "Import items"
      pattern: "import.*CATALOG_ITEMS"
</must_haves>

<output>
Create `.planning/phases/01-3d-workspace-configurator/01-02-SUMMARY.md` when done
</output>