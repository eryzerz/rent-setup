---
phase: 01-3d-workspace-configurator
plan: "03"
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - src/components/workspace/SceneContent.tsx
  - src/components/workspace/FurnitureModel.tsx
  - src/components/workspace/SelectedItemsBar.tsx
  - src/components/workspace/WorkspaceConfigurator.tsx
  - src/components/workspace/ViewportCanvas.tsx
autonomous: true
requirements:
  - REQ-3D-01
  - REQ-3D-03
  - REQ-3D-04
  - REQ-3D-05
user_setup: []
---

<objective>
Wire the 3D scene to Zustand store, implement auto-positioning with predefined slots, build the selected items bar at bottom of screen.

Purpose: Connect user selections in the catalog to visual 3D representation — the core value proposition.
Output: Items appear in 3D scene at predefined positions when selected, selected items bar shows current selections.
</objective>

<context>
@.planning/phases/01-3d-workspace-configurator/01-RESEARCH.md (Pattern 2: useGLTF, Pattern 3: Zustand, Pattern 6: Catalog Data)
@.planning/phases/01-3d-workspace-configurator/01-02-PLAN.md (Wave 2 deliverables)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SceneContent component inside R3F Canvas</name>
  <files>src/components/workspace/SceneContent.tsx</files>
  <action>
Create SceneContent component (inside Canvas, so it can use R3F hooks):
- 'use client' directive
- Uses useConfiguratorStore selectors to read: selectedTable, selectedChair, selectedMonitors, selectedDecoratives, selectedShelf
- Renders FurnitureModel for each selected item
- Uses item.slotPosition from catalog data for positioning
- Renders floor plane for ContactShadows

This is the bridge between Zustand state and 3D rendering per Pattern 3.
  </action>
  <verify>
<automated>grep -q "useConfiguratorStore" src/components/workspace/SceneContent.tsx</automated>
  </done>
  <done>SceneContent reads Zustand selection and renders models</done>
</task>

<task type="auto">
  <name>Task 2: Create FurnitureModel component</name>
  <files>src/components/workspace/FurnitureModel.tsx</files>
  <action>
Create FurnitureModel component:
- 'use client' directive
- Uses useGLTF to load model from url prop
- Applies position prop to <group>
- Traverse scene to set castShadow/receiveShadow on all meshes
- Returns <primitive object={scene} /> wrapped in group
- Fallback to PlaceholderModel if model doesn't exist (box with category color)

Follow Pattern 2 from RESEARCH.md.
  </action>
  <verify>
<automated>grep -q "useGLTF" src/components/workspace/FurnitureModel.tsx</automated>
  </done>
  <done>FurnitureModel loads GLTF and renders with position/scale</done>
</task>

<task type="auto">
  <name>Task 3: Connect ViewportCanvas to SceneContent</name>
  <files>src/components/workspace/ViewportCanvas.tsx</files>
  <action>
Update ViewportCanvas to include SceneContent component inside Canvas. Wrap SceneContent in Suspense with loading fallback.
  </action>
  <verify>
<automated>grep -q "SceneContent" src/components/workspace/ViewportCanvas.tsx</automated>
  </done>
  <done>ViewportCanvas renders SceneContent inside Canvas</done>
</task>

<task type="auto">
  <name>Task 4: Implement monitor arm positioning logic</name>
  <files>src/components/workspace/SceneContent.tsx</files>
  <action>
Ensure monitor items use slotPosition from catalog data. The catalog data already defines MONITOR_SLOTS array with 3 positions. When rendering selectedMonitors array, use index to get position: monitor.slotPosition (predefined per item) or index-based for multiple monitors on arm.

Verify monitors appear in a row on the "monitor arm" area (y ~ 0.75).
  </action>
  <verify>
<automated>grep -q "selectedMonitors" src/components/workspace/SceneContent.tsx</automated>
  </done>
  <done>Monitors render at predefined positions in a row</done>
</task>

<task type="auto">
  <name>Task 5: Create SelectedItemsBar component</name>
  <files>src/components/workspace/SelectedItemsBar.tsx</files>
  <action>
Create SelectedItemsBar component:
- Fixed bottom strip, height ~64px, light zone background
- Horizontal scrollable row of selected item thumbnails
- Uses useConfiguratorStore.getSelectedItems() to get all selections
- Each item shows: thumbnail, name, X button to remove
- "Rent" button enabled when itemCount > 0
- Clicking "Rent" opens SummaryModal

Follow Pattern 5 from RESEARCH.md for visual design.
  </action>
  <verify>
<automated>grep -q "SelectedItemsBar" src/components/workspace/SelectedItemsBar.tsx</automated>
  </done>
  <done>SelectedItemsBar shows current selections with remove buttons</done>
</task>

<task type="auto">
  <name>Task 6: Integrate SelectedItemsBar into WorkspaceConfigurator</name>
  <files>src/components/workspace/WorkspaceConfigurator.tsx</files>
  <action>
Update WorkspaceConfigurator bento layout to include SelectedItemsBar:
- Column flex layout for right side: ViewportCanvas (flex-1) + SelectedItemsBar (64px)
- Bottom bar spans full width (under sidebar too)

Layout: Sidebar | (3D viewport + bottom bar)
  </action>
  <verify>
<automated>grep -q "SelectedItemsBar" src/components/workspace/WorkspaceConfigurator.tsx</automated>
  </done>
  <done>WorkspaceConfigurator includes bottom selected items bar</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Zustand→R3F | State changes trigger re-render — R3F handles efficiently with selectors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Denial | R3F frame drops | accept | Local prototype, no performance requirements |
</threat_model>

<verification>
- Select item in catalog → appears in 3D scene at predefined position
- Select another table → replaces previous table in scene
- Select up to 3 monitors → appear in a row at monitor arm position
- SelectedItemsBar shows all selected items with thumbnails
- Remove button in bar removes item from selection
- Rent button enables when items selected
</verification>

<success_criteria>
- [x] 3D scene updates when catalog selection changes
- [x] Items position automatically based on slotPosition in catalog data
- [x] Same-category replacement reflects in 3D scene
- [x] Monitor arm supports up to 3 monitors at predefined positions
- [x] SelectedItemsBar displays all current selections
- [x] Remove button in bar works correctly
- [x] Rent button enables when items selected
</success_criteria>

<must_haves>
  truths:
    - "User sees selected items in 3D scene instantly"
    - "Items auto-position at predefined slots"
    - "SelectedItemsBar shows all current selections"
  artifacts:
    - path: "src/components/workspace/SceneContent.tsx"
      provides: "Bridges Zustand selection state to 3D rendering"
    - path: "src/components/workspace/FurnitureModel.tsx"
      provides: "Loads GLTF models with useGLTF, applies position"
    - path: "src/components/workspace/SelectedItemsBar.tsx"
      provides: "Bottom strip showing all selections, rent button"
  key_links:
    - from: "SceneContent"
      to: "useConfiguratorStore"
      via: "selectors"
      pattern: "useConfiguratorStore.*selected"
    - from: "SceneContent"
      to: "FurnitureModel"
      via: "render"
      pattern: "FurnitureModel.*position"
    - from: "SelectedItemsBar"
      to: "useConfiguratorStore"
      via: "getSelectedItems"
      pattern: "useConfiguratorStore.*getSelectedItems"
</must_haves>

<output>
Create `.planning/phases/01-3d-workspace-configurator/01-03-SUMMARY.md` when done
</output>