---
phase: 01-3d-workspace-configurator
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - tailwind.config.js
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/lib/utils.ts
  - src/components/ui/button.tsx
  - src/components/ui/card.tsx
  - src/components/ui/accordion.tsx
  - src/components/ui/dialog.tsx
  - src/components/ui/badge.tsx
  - src/types/catalog.ts
  - src/stores/useConfiguratorStore.ts
  - src/components/workspace/WorkspaceConfigurator.tsx
  - src/components/workspace/ViewportCanvas.tsx
  - src/components/catalog/catalogData.ts
autonomous: true
requirements:
  - REQ-3D-01
  - REQ-3D-02
user_setup:
  - service: npm
    why: "Install dependencies for Next.js, R3F, shadcn/ui"
    env_vars: []
    dashboard_config: []
---

<objective>
Initialize Next.js project with React Three Fiber, configure shadcn/ui, set up Zustand store, and create basic scene infrastructure with placeholder 3D models.

Purpose: Establish foundation for all subsequent phases — no dependency work needed for later waves.
Output: Working Next.js app with R3F canvas, Zustand store, and placeholder models rendering in 3D scene.
</objective>

<context>
@.planning/phases/01-3d-workspace-configurator/01-RESEARCH.md (Stack, Architecture Patterns 1-7)
@.planning/phases/01-3d-workspace-configurator/01-CONTEXT.md (Phase Boundary, Categories)
@design-system/design-system.md (Colors, Typography, Spacing, shadcn mapping)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js project with dependencies</name>
  <files>package.json, tsconfig.json, tailwind.config.js</files>
  <action>
Initialize Next.js project and install all dependencies. Run:
```bash
npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --yes
npm install three @react-three/fiber @react-three/drei zustand lucide-react class-variance-authority clsx tailwind-merge
npx shadcn@latest init --yes
npx shadcn@latest add button card accordion dialog badge --yes
```

This creates package.json with all required dependencies.
  </action>
  <verify>
<automated>npm run build</automated>
  </verify>
  <done>Next.js app builds without errors, all dependencies installed</done>
</task>

<task type="auto">
  <name>Task 2: Configure design system tokens</name>
  <files>tailwind.config.js</files>
  <action>
Add design system tokens to tailwind.config.js. Merge the design-system colors with shadcn defaults. Use the light zone colors (bg-gray-50, bg-white, text-gray-900) for the sidebar. Set dark viewport background in ViewportCanvas component.
  </action>
  <verify>
<automated>grep -q "bg-gray-50" tailwind.config.js || echo "Config needs colors"</automated>
  </verify>
  <done>Tailwind config includes design system tokens</done>
</task>

<task type="auto">
  <name>Task 3: Create catalog types and data</name>
  <files>src/types/catalog.ts, src/components/catalog/catalogData.ts</files>
  <action>
Create TypeScript types for CatalogItem, Category, SlotDefinition. Define catalog items for all 5 categories (<5 each):
- Tables (3 items): standard desk, L-shaped, compact
- Chairs (3 items): ergonomic, executive, casual
- Monitors (3 items): 27" standard, 32" 4K, ultrawide
- Decorative (4 items): desk lamp, floor lamp, plant, clock
- Shelves (3 items): wall-mounted, standing, corner

Each item includes: id, name, category, price (monthly), modelUrl (/models/...), thumbnail, slotPosition [x,y,z], scale. Define MONITOR_SLOTS array with 3 positions for monitor arm.
  </action>
  <verify>
<automated>grep -c "id:" src/components/catalog/catalogData.ts</automated>
  </verify>
  <done>16 catalog items defined with prices and 3D positions</done>
</task>

<task type="auto">
  <name>Task 4: Create Zustand store</name>
  <files>src/stores/useConfiguratorStore.ts</files>
  <action>
Create Zustand store with:
- selectedTable: CatalogItem | null
- selectedChair: CatalogItem | null  
- selectedMonitors: CatalogItem[] (max 3)
- selectedDecoratives: CatalogItem[]
- selectedShelf: CatalogItem | null
- selectItem(item) - handles category-specific logic
- removeItem(itemId)
- clearAll()
- getTotalPrice() - derives total from selected items
- getSelectedItems() - returns flat array of all selected

Use selectors pattern for R3F compatibility. Store primitives only (no Three.js objects).
  </action>
  <verify>
<automated>grep -c "useConfiguratorStore" src/stores/useConfiguratorStore.ts</automated>
  </verify>
  <done>Zustand store with selection logic, price calculation, state management</done>
</task>

<task type="auto">
  <name>Task 5: Create shadcn/ui components</name>
  <files>src/components/ui/button.tsx, src/components/ui/card.tsx, src/components/ui/accordion.tsx, src/components/ui/dialog.tsx, src/components/ui/badge.tsx</files>
  <action>
Ensure all shadcn/ui components are installed and configured. These were added in Task 1. Verify they use design-system tokens and follow shadcn conventions.
  </action>
  <verify>
<automated>ls -la src/components/ui/ | wc -l</automated>
  </verify>
  <done>shadcn/ui components present in src/components/ui/</done>
</task>

<task type="auto">
  <name>Task 6: Create main workspace component with dynamic import</name>
  <files>src/app/page.tsx, src/components/workspace/WorkspaceConfigurator.tsx</files>
  <action>
Create src/app/page.tsx with dynamic import for client component:
```typescript
import dynamic from 'next/dynamic';
const WorkspaceConfigurator = dynamic(
  () => import('@/components/workspace/WorkspaceConfigurator').then(m => m.WorkspaceConfigurator),
  { ssr: false, loading: () => <div>Loading...</div> }
);
```

Create WorkspaceConfigurator.tsx - the main client wrapper that will contain: CatalogSidebar, ViewportCanvas, SelectedItemsBar, SummaryModal. Start with a placeholder shell structure.
  </action>
  <verify>
<automated>grep -q "dynamic" src/app/page.tsx</automated>
  </done>
  <done>Page.tsx uses dynamic import with ssr:false, WorkspaceConfigurator wrapper exists</done>
</task>

<task type="auto">
  <name>Task 7: Create ViewportCanvas with R3F scene</name>
  <files>src/components/workspace/ViewportCanvas.tsx</files>
  <action>
Create ViewportCanvas component:
- 'use client' directive
- Uses @react-three/fiber Canvas with dark background (#0a0a0a)
- Includes OrbitControls, Environment preset="city", ContactShadows
- Camera position: [4, 3, 4], fov: 50
- Contains placeholder floor mesh for shadows

Follow Pattern 1 from RESEARCH.md for R3F + Next.js integration.
  </action>
  <verify>
<automated>grep -q "OrbitControls" src/components/workspace/ViewportCanvas.tsx</automated>
  </done>
  <done>ViewportCanvas renders R3F Canvas with orbit controls and dark background</done>
</task>

<task type="auto">
  <name>Task 8: Map existing models to catalog data</name>
  <files>public/*.glb, src/components/catalog/catalogData.ts</files>
  <action>
Map existing .glb files in public/ to catalog items:
- Tables: computer_desk.glb, computer_desk(1).glb, table_desk_-_3mb.glb
- Chairs: gaming_chair.glb, office_chair.glb, monobloc_plastic_garden_chair.glb
- Monitors: monitor.glb, curved_monitor.glb, ultrawide_monitor.glb
- Decorative: desk_lamp.glb, sci-fi_desk_lamp.glb, assignment_8_plant.glb, pencil_holder.glb
- Shelves: bookshelf.glb, bookshelf(1).glb, bookshelf(2).glb

Update src/components/catalog/catalogData.ts with modelUrl pointing to actual files:
- modelUrl: "/computer_desk.glb" (Tables)
- modelUrl: "/gaming_chair.glb" (Chairs)
- modelUrl: "/monitor.glb" (Monitors)
- etc.

Define appropriate slot positions for each category. Store prices in cents to avoid floating point issues (e.g., 4999 = $49.99/month).
  </action>
  <verify>
<automated>grep -c "\.glb" src/components/catalog/catalogData.ts</automated>
  </verify>
  <done>17 real 3D models mapped to catalog categories with slot positions</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→npm | Untrusted npm packages installed — slopcheck passed in RESEARCH.md |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | npm install | mitigate | All packages verified on npm registry, slopcheck passed per RESEARCH.md package audit table |
</threat_model>

<verification>
- npm run build completes without errors
- npm run dev starts dev server on localhost:3000
- Open browser to verify R3F canvas renders with dark background
- Orbit controls allow rotation/zoom
</verification>

<success_criteria>
- [x] Next.js project runs with dev server
- [x] shadcn/ui components installed (button, card, accordion, dialog, badge)
- [x] Zustand store created with selection logic
- [x] Catalog types defined, 16 items with prices
- [x] ViewportCanvas renders R3F scene with dark background
- [x] OrbitControls work in browser
- [x] Placeholder models or boxes render in scene
</success_criteria>

<must_haves>
  truths:
    - "Project initializes and builds without errors"
    - "R3F Canvas renders with dark background and orbit controls"
    - "Zustand store holds selection state"
    - "Catalog data exists with 16 items across 5 categories"
  artifacts:
    - path: "package.json"
      provides: "Dependencies: next, react, three, r3f, drei, zustand, shadcn"
    - path: "src/stores/useConfiguratorStore.ts"
      provides: "Selection state, price calculation, add/remove/clear actions"
    - path: "src/components/workspace/ViewportCanvas.tsx"
      provides: "R3F scene with Canvas, OrbitControls, Environment, ContactShadows"
    - path: "src/components/catalog/catalogData.ts"
      provides: "16 catalog items with prices and slot positions"
  key_links:
    - from: "WorkspaceConfigurator.tsx"
      to: "ViewportCanvas.tsx"
      via: "Import"
      pattern: "import.*ViewportCanvas"
    - from: "ViewportCanvas.tsx"
      to: "useConfiguratorStore.ts"
      via: "Store selector"
      pattern: "useConfiguratorStore.*selected"
</must_haves>

<output>
Create `.planning/phases/01-3d-workspace-configurator/01-01-SUMMARY.md` when done
</output>