---
phase: 01-3d-workspace-configurator
plan: "04"
type: execute
wave: 4
depends_on:
  - "01-03"
files_modified:
  - src/components/workspace/SummaryModal.tsx
  - src/components/workspace/WorkspaceConfigurator.tsx
autonomous: true
requirements:
  - REQ-3D-06
  - REQ-3D-07
user_setup: []
---

<objective>
Build the summary modal with item list, total price, and rent confirmation. Show success message after confirm.

Purpose: Users review their selection, see pricing, and confirm rental — completes the core user flow.
Output: Summary modal with item grid, price, and success state.
</objective>

<context>
@.planning/phases/01-3d-workspace-configurator/01-RESEARCH.md (Pattern 5: shadcn Dialog for Summary Modal)
@.planning/phases/01-3d-workspace-configurator/01-03-PLAN.md (Wave 3 deliverables)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SummaryModal component</name>
  <files>src/components/workspace/SummaryModal.tsx</files>
  <action>
Create SummaryModal component:
- 'use client' directive
- Uses shadcn/ui Dialog for overlay
- Props: open (boolean), onOpenChange (callback), onRent (callback)
- Reads selected items via useConfiguratorStore.getSelectedItems()
- Reads total via useConfiguratorStore.getTotalPrice()

Content:
- DialogHeader with title "Your Workspace Setup"
- Grid of Card components showing each selected item (thumbnail, name, category badge, price)
- Border-top section showing "Monthly Total" and total price
- Confirm Rental button

Follow Pattern 5 from RESEARCH.md.
  </action>
  <verify>
<automated>grep -q "Dialog" src/components/workspace/SummaryModal.tsx</automated>
  </done>
  <done>SummaryModal renders Dialog with item list and total</done>
</task>

<task type="auto">
  <name>Task 2: Implement rent confirmation flow</name>
  <files>src/components/workspace/SummaryModal.tsx</files>
  <action>
Add confirmation state to SummaryModal:
- Initial state: show item list and Confirm button
- User clicks "Confirm Rental" → show success message
- Success message: "Rent completed! We'll contact you with shipping details."
- On success, also clear all selections via store.clearAll()
- Close modal after success

No actual payment processing per requirements - just success message.
  </action>
  <verify>
<automated>grep -q "clearAll" src/components/workspace/SummaryModal.tsx</automated>
  </done>
  <done>Confirm button triggers success state and clears selection</done>
</task>

<task type="auto">
  <name>Task 3: Wire SummaryModal into WorkspaceConfigurator</name>
  <files>src/components/workspace/WorkspaceConfigurator.tsx</files>
  <action>
Update WorkspaceConfigurator:
- Add open state for SummaryModal
- Pass open and onOpenChange to SummaryModal
- Rent button in SelectedItemsBar sets modal open = true
- SummaryModal renders via Dialog (portal, doesn't need layout placement)

Layout adds modal as overlay, not part of flex flow.
  </action>
  <verify>
<automated>grep -q "SummaryModal" src/components/workspace/WorkspaceConfigurator.tsx</automated>
  </done>
  <done>SummaryModal opens from Rent button, closes on confirm</done>
</task>

<task type="auto">
  <name>Task 4: Polish UI and edge cases</name>
  <files>src/components/workspace/SelectedItemsBar.tsx, src/components/workspace/SummaryModal.tsx</files>
  <action>
Polish the UI:
- Empty state: When no items selected, show "Select items from the catalog" in SelectedItemsBar
- Rent button disabled state when itemCount === 0
- Summary modal empty state handling
- Smooth transitions/animations
- Design system colors applied throughout

Edge cases:
- 0 items: Rent button disabled
- 1+ items: Rent button enabled
- Monitor selection: show "x3" if 3 monitors selected in list
  </action>
  <verify>
<automated>npm run build 2>&1 | tail -20</automated>
  </done>
  <done>All UI polished, build passes, edge cases handled</done>
</task>

<task type="auto">
  <name>Task 5: Final verification and cleanup</name>
  <files>src/app/page.tsx</files>
  <action>
Run final verification:
- npm run build completes without errors
- npm run dev starts server
- Browser test: select items from all categories
- Verify 3D scene updates correctly
- Verify selected items bar shows selections
- Verify summary modal opens with correct items and price
- Verify confirm shows success message

Clean up any console warnings or unused imports.
  </action>
  <verify>
<automated>npm run build</automated>
  </verify>
  <done>Build passes, all features working in browser</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User→Modal | Click confirm shows success — no actual backend/payment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information | Success message | accept | No sensitive data displayed |
</threat_model>

<verification>
- Click "Rent" button opens SummaryModal
- Modal shows all selected items with thumbnails
- Total price calculated correctly
- Click "Confirm Rental" shows success message
- Success message: "Rent completed! We'll contact you with shipping details."
- After confirm, selections cleared, modal closes
- All design system tokens applied correctly
</verification>

<success_criteria>
- [x] SummaryModal opens from Rent button click
- [x] Modal shows item list with thumbnails, names, prices
- [x] Total price displays correctly
- [x] Confirm triggers success message
- [x] Success clears selections and closes modal
- [x] Rent button disabled when no items selected
- [x] Design system colors used throughout
- [x] Build passes without errors
</success_criteria>

<must_haves>
  truths:
    - "User can review all selections with prices"
    - "User can confirm rental and see success message"
  artifacts:
    - path: "src/components/workspace/SummaryModal.tsx"
      provides: "Item list, total price, confirm button, success state"
    - path: "src/components/workspace/WorkspaceConfigurator.tsx"
      provides: "Modal integration with Rent button"
  key_links:
    - from: "SelectedItemsBar"
      to: "SummaryModal"
      via: "Rent button onClick"
      pattern: "setModalOpen(true)"
    - from: "SummaryModal"
      to: "useConfiguratorStore"
      via: "getSelectedItems, getTotalPrice, clearAll"
      pattern: "useConfiguratorStore.*getSelectedItems"
</must_haves>

<output>
Create `.planning/phases/01-3d-workspace-configurator/01-04-SUMMARY.md` when done
</output>